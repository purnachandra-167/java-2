name: Docker Image Build

on:
  push:
    branches: [ "swagatika" ]
  

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag  newdockerimage:$(date +%s)
